<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aq.substratedemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--application指定substrate入口类-->
        <meta-data android:name="com.saurik.substrate.main"
            android:value="aq.substratedemo.subMain" ></meta-data>
    </application>
    <!-- 权限声明为substrate组件 -->
    <uses-permission android:name="cydia.permission.SUBSTRATE"></uses-permission>


</manifest>